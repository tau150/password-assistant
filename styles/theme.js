import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  colors: {
    brand: {
      primary: "#FF0049",
      secondary: "#002B45",
      terciary: "#FFF1E5"
    },
  },
})

export default theme;