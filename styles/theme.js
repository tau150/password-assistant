import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  colors: {
    brand: {
      primary: "#FF0049",
      secondary: "#002B45",
      terciary: "#FFF1E5",
      accent: '#788f9d'
    },
  },
})

export default theme